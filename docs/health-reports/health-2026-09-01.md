# VetPath Weekly Health Check - 2026-09-01 (evening, Kaleb-requested)

**Verdict: PASS - all systems green.** The two standing human calls from the
Aug 31 scheduled report remain open (OneDrive vs `.next`, untracked files);
nothing new is broken.

## 1. Production URLs - PASS
11/11 return 200: /, all four /guides/ pages, /trust, /onboarding, /benefits,
/timeline, /privacy, /sitemap.xml.

## 2. Data integrity - PASS (7/7)
All six data files pass shape checks; INTAKE_STEP_EVENTS matches the intake
steps in order (basics, status, priorities, weights, goals).

## 3. Types & build - PASS
`tsc --noEmit` clean. Three separate clean production builds today (bug check,
perf pass, release refresh) - no recurrence of yesterday's one-off
`pages-manifest.json` ENOENT. The Aug 31 flag stands: **the OneDrive-vs-.next
exclusion is still an open human call** (third distinct sync-related failure on
record; runbook only protects `dev`, not `build`).

## 4. Supabase - PASS
- Cron `vetpath-checkins` active; email_log holds 26 sends (15 on Aug 27,
  11 on Aug 31), zero failures.
- Return ledger: 7 visit-days across 5 signed-in users; **2 users have now
  returned on 2+ distinct days** (was 1 at the Week Two pull yesterday - the
  retention number is moving the right way).
- journal_entries: 0 rows - expected; the dashboard doorway shipped tonight
  (d280520). Next week's check is the first fair read.
- Security advisors: only the accepted leaked-password WARN (Pro declined).

## 5. Source-link sample - PASS
6/6 randomly sampled state benefit sources returned direct 200s (MT, RI, KY,
CT, OH, DE). No bot-walls hit this round.

## 6. Deliverables - PASS
All four deliverables refreshed tonight (cc4f0f8): share app zip + Desktop
copy, website zip (LCP preload verified inside), artifact republished,
preview copy. Nothing stale.

## 7. Housekeeping - DONE
- Versioned builds: only `.next-v68` exists - nothing to prune.
- Deleted 6 stale demo mirror checkpoints (all corresponded to long-verified,
  shipped mirrors; protocol says delete after verification passes).

## 8. Git - FLAG (carried)
The Aug 31 untracked-files flag stands: ~15 untracked files older than the
last several commits, notably `tsconfig.ruletest.json` (Jul 27) and
`docs/llm-assistant-spec.md` (Jul 14), which look like real work. Human call:
commit, gitignore, or delete.

## This week's ships (Aug 26 - Sep 1)
Retention Wave 2, Personality Pass (8/8), farOut rules fix, funnel
instrumentation + phone audit, PhaseNow + journal prompts, public guides SEO
push (4 pages), transparent ribbon restored (site + demo), first-touch
attribution hardened (in-app browsers + OAuth masking), journal doorway
(site + demo), phone performance pass (Lighthouse mobile 34 -> mid-60s,
CLS 0.321 -> 0.001, transfer -600KB), Week Two snapshot finalized, Sep 1
early funnel reading recorded (4/4 completions, no leak at n=4).
