# VetPath Weekly Health Check — 2026-08-31

**Verdict: PASS with 3 flags** — nothing broken in the product; a flaky build, a bot-blocked source link, and a pile of long-untracked files need a human call.

## 1. Build — FLAG (intermittent)
- `npx tsc --noEmit` → exit 0. Clean.
- First clean `npm run build` **failed** after "Compiled successfully" with
  `ENOENT ... .next\server\pages-manifest.json`. Second identical run (same clean
  `.next`) → exit 0, all 30+ routes prerendered. Not a code error — the compile and
  type-check both passed and only the manifest read failed, which points at OneDrive
  sync touching `.next` mid-build.
- **This is the third distinct OneDrive-vs-`.next` failure on record**, and the first
  that hits `build` rather than `dev`:
  - 2026-07-13 — dev start died on `EINVAL readlink '.next\package.json'` (4 attempts).
  - 2026-08-17 — same `readlink` failure; produced the "build → `rm -rf .next` → dev"
    runbook rule.
  - 2026-08-31 (today) — `build` itself fails reading `pages-manifest.json`.
  The existing runbook rule only protects `dev`. Nothing protects `build`.
  **Human call:** exclude `.next` (and `.next-v*`) from OneDrive sync, or move the
  repo out of the synced folder. A build that fails ~50% of the time will eventually
  fail a deploy, and the error names a missing file rather than the real cause — which
  is exactly the kind of thing that burns an hour at the wrong moment.
- Dev server on :3000 killed before build (PID 10132), restarted after via a detached
  `Start-Process` (the method the 2026-07-13 report established) — up first try,
  `GET http://localhost:3000/` → 200.

## 2. Data integrity — PASS
| File | Expected | Found |
|---|---|---|
| stateBenefits.json | 51 states | 51 |
| sampleBenefits.json | 11 categories, each w/ lastVerified + sources | 11, all complete |
| sampleCareers.json | 15 careers w/ onetCode + blsUrl | 15, all complete |
| relocationMetros.json | 29 metros, 28 w/ `official` | 29, 28 (`rural-telehealth` is the expected exception) |
| assessmentQuestions.json | 11 questions, "wins" first | 11, `wins` first |

- **Intake funnel sync — OK.** `lib/track.ts` `INTAKE_STEP_EVENTS`
  (`intake-1-basics … intake-5-goals`) matches `data/intakeQuestions.json` step ids
  (`basics, status, priorities, weights, goals`) one-to-one and in order. No silent
  funnel drop-off.

## 3. Link-rot sample (8 of 395 unique source URLs) — FLAG (1 unverifiable)
7/8 loaded as the correct page, no unexpected redirects: AL scholarship, GA business
cert exemption, MD property tax, NE military retirement exclusion, PA veterans'
preference, WA property tax relief, BLS series `LAUMT473498000000003` (Nashville MSA,
data through June 2026).

- `https://www.dfas.mil/retiredmilitary/` → **HTTP 403**, also 403 with a browser
  user agent. This reads as a DFAS bot/datacenter block, not a dead link — but it
  cannot be verified from automation. **Human call:** open it in a normal browser once
  to confirm it's alive.

## 4. Deliverables freshness — PASS
`share/VetPath-Interactive-App.zip` (Aug 31 00:02) is newer than
`demo/vetpath-demo.html` (Aug 31 00:01). Share package is current.

## 5. Staleness — PASS
14 `lastVerified` fields across `data/`. Oldest is **61 days** (`funding.json`,
`reserves.json` at `2026-07`); the benefit data sits at 54 days (`2026-07-08`).
Nothing past the 100-day line. Quarterly refresh not yet due — next window opens
around **2026-10-08** when the benefits data crosses 100 days.
- Minor: `funding.json` and `reserves.json` use month-precision `2026-07` while every
  other file uses full `YYYY-MM-DD`. Harmless today, but it makes the age ambiguous by
  up to 30 days.

## 6. Git — FLAG
Last commit `46a590b` (2026-08-31 00:37). **15 untracked files, every one older than
that commit** — they've survived multiple commits uncommitted:
- `tsconfig.ruletest.json` (Jul 27) and `docs/llm-assistant-spec.md` (Jul 14) — these
  look like real work, not scratch.
- `.claude/launch.json` (Jul 13)
- `public/img/dvids-*.jpg` (Jul 8), `public/video/career-summit-*` (Jul 10) — assets
  the site may reference.
- `share/cap-*.{png,jpeg}`, `share/VetPath-Preview.html`, `share/verify-preview.png`
  (Jul 9) — likely stale share captures.

**Human call:** decide commit vs. gitignore vs. delete, file by file. Not touched here.

## 7. Housekeeping — PASS
Found `.next-v67`, `.next-v68`. Deleted **1** (`.next-v67`, 7.2 MB reclaimed), kept the
highest `.next-v68`.

---
*Automated run. Nothing fixed beyond the dev-server restart and the pre-authorized
`.next-v*` prune.*
