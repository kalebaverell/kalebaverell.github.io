# VetPath Weekly Health Check — 2026-07-20

**Verdict: HEALTHY — one flag.** Build, data, and links all clean; an unshipped "funded path" feature has been sitting uncommitted in the working tree for 4 days.

---

## 1. Build — PASS
- `npx tsc --noEmit` → exit 0, no errors.
- Ports 3000–3002 were already free (no stray dev servers).
- `rm -rf .next` + `npm run build` → exit 0. 23 routes, all prerendered static. Shared JS 87.5 kB.
- `npm run dev` restarted, serving HTTP 200 on **port 3000**.

**Note for the humans:** the first `npm run dev` after the build crashed with `EINVAL: readlink .next/package.json` — a production `.next` is not reusable by dev. Clearing `.next` a second time fixed it. The existing rule is "never run build and dev simultaneously"; it should probably be extended to "always clear `.next` when switching between build and dev." Worth adding to the health-check procedure.

## 2. Data Integrity — PASS
| File | Expected | Found |
|---|---|---|
| `stateBenefits.json` | 51 states | 51 ✓ (`coverage.missing` is empty) |
| `sampleBenefits.json` | 11 categories, each w/ lastVerified + sources | 11 ✓, zero missing fields |
| `sampleCareers.json` | 15 careers w/ onetCode + blsUrl | 15 ✓, zero missing either field |
| `relocationMetros.json` | 29 metros, 28 with `official` | 29 ✓, 28 with `official` ✓ |
| `assessmentQuestions.json` | 10 questions | 10 ✓ |

The one metro without an `official` block is "Rural + telehealth pattern" — a generic pattern entry, not a real metro, so having no official source is correct.

## 3. Link-Rot Sample — PASS (8/8 resolve to correct content)
Sampled deterministically across all 408 source URLs in the data files.

| URL | Result |
|---|---|
| admin.ks.gov — KS veterans preference | 200, correct page |
| dor.mo.gov — MO Military Reference Guide (PDF) | 200, 476 kB PDF served |
| oklahoma.gov/veterans/benefits.html | 200, correct page |
| veterans.alaska.gov — veteran preference | 200, correct page |
| vets.colorado.gov | **403 → 200** (see below) |
| hesc.ny.gov — NYS Veterans Tuition Award | **301 → 200** (see below) |
| nj.gov — NJ veteran tax exemption | 200, correct page ($6,000 exemption) |
| va.gov — transfer Post-9/11 GI Bill | 200, correct page |

Two apparent anomalies, both benign after follow-up:
- **vets.colorado.gov** returns 403 to a bare request but **200 with a normal browser User-Agent** — bot-filtering, not a dead link. No action needed.
- **hesc.ny.gov** 301-redirects `www` → apex host and lands on the correct "Veterans Tuition Award | HESC" program page, not a generic landing page. Optionally update the stored URL to drop `www.` to avoid the hop.

## 4. Deliverables Freshness — PASS
- `demo/vetpath-demo.html` — 2026-07-10 14:30
- `share/VetPath-Interactive-App.zip` — 2026-07-10 17:55

Zip is ~3.5 hours **newer** than the demo. Share package is current; no rebuild needed.

## 5. Staleness — PASS
All `lastVerified` dates are 2026-07-08 / 2026-07-09 — **11–12 days old**, well inside the 100-day window. `stateBenefits.json` is stamped 2026-07-08 (12 days). Next quarterly refresh is not due until roughly mid-October 2026.

## 6. Git — FLAG
Last commit: `62af093` (2026-07-14) "Intake: multi-state residence (select all that apply)".

Uncommitted work **newer than the last commit**, all timestamped 2026-07-16 ~14:30:

```
 M app/dashboard/page.tsx
 M app/pathfinder/page.tsx
?? components/FundedPath.tsx
?? data/funding.json
?? lib/funding.ts
```

These five files are a coherent unshipped feature — a funding/"funded path" module wired into both the dashboard and pathfinder pages. It has been uncommitted for 4 days and includes **two untracked files with no backup in git history**. Recommend committing or stashing it.

Also untracked, lower priority:
- `docs/health-reports/` — this report directory itself, never committed
- `docs/llm-assistant-spec.md` (2026-07-14)
- `.claude/launch.json` (2026-07-13)

---

*Nothing was modified beyond restarting the dev server. All other findings are flagged for human action.*
