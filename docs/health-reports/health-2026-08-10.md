# VetPath Weekly Health Check — 2026-08-10

**Verdict: PASS with 3 flags** — code and data are clean; the share package and demo are 4 days behind uncommitted app changes, and two source URLs couldn't be verified from this environment.

---

## 1. BUILD — PASS
- `npx tsc --noEmit` → exit 0, no errors.
- Ports 3000–3002: all free before build (nothing to kill).
- `.next` removed, `npm run build` → exit 0. 28 routes, all prerendered static. Shared first-load JS 87.5 kB.
- Dev server restarted on :3000 afterward (`vetpath-dev`). Homepage loads, title `VetPath - A clear gameplan for life after service`, zero console errors.

## 2. DATA INTEGRITY — PASS
| File | Expected | Found |
|---|---|---|
| `stateBenefits.json` | 51 states | 51, all named |
| `sampleBenefits.json` | 11 categories, each w/ `lastVerified` + `sources` | 11, all have both |
| `sampleCareers.json` | 15 careers w/ `onetCode` + `blsUrl` | 15, all have both |
| `relocationMetros.json` | 29 metros, 28 w/ `official` | 29 metros, 28 `official` (missing: `rural-telehealth` — expected) |
| `assessmentQuestions.json` | 10 questions | 10, all with `id` + `label` + `options` |

## 3. LINK-ROT SAMPLE — FLAG (2 of 8 unverifiable, 0 confirmed dead)
| # | URL | Result |
|---|---|---|
| 1 | veterans.arkansas.gov | OK — AR DVA homepage |
| 2 | veterans.idaho.gov | OK — Idaho DVS homepage |
| 3 | mn.gov/mdva/…/minnesotagibill/ | OK — "Minnesota GI Bill" |
| 4 | veterans.ny.gov/property-tax-exemptions-veterans | OK — correct topic page |
| 5 | dvs.virginia.gov/…/virginia-values-veterans-v3-program/ | **403** |
| 6 | va.gov/pension/ | OK — "VA pension benefits" |
| 7 | bls.gov/ooh/management/top-executives.htm | **403** |
| 8 | apps.bea.gov/regional/zip/MARPP.zip | OK — live zip, contains `MARPP_MSA_2008_2024.csv` |

**FLAG:** #5 and #7 return 403 (not 404, no redirect) — same result with a browser user-agent, so this reads as edge/WAF bot-blocking rather than link rot. `bls.gov` matters disproportionately: `blsUrl` is on all 15 careers. **Needs a human to open both in a real browser to confirm they're fine.**

## 4. DELIVERABLES FRESHNESS — PASS (with caveat below)
- `demo/vetpath-demo.html` → 2026-08-05 10:04:27
- `share/VetPath-Interactive-App.zip` → 2026-08-05 10:07:28 (**newer** — share package not stale vs. demo)

**Caveat (see §6):** both were built on Aug 5. Twelve app/lib/component source files changed Aug 9 20:39–20:47 and have **not** been mirrored into either the demo or the share package. The §4 test as specified passes, but neither deliverable reflects current `main`.

## 5. STALENESS — PASS
13 `lastVerified` fields, 4 distinct values, oldest `2026-07-08` (**33 days**). Nothing near the 100-day threshold. Quarterly refresh not due until ~2026-10-16.

## 6. GIT — FLAG
Last commit: `ff81b4b` 2026-08-05 10:07:10 — *Mirror salary-aware Pathfinder + copy pass into the demo*

- **13 tracked files modified, uncommitted.** Ten `app/*/page.tsx` + `components/Nav.tsx` + `lib/timeline.ts` + `data/intakeQuestions.json`, all touched Aug 9 20:39–20:47 — a full day's work sitting uncommitted. Also `share/VetPath-Interactive-App.{html,zip}` and `share/VetPath-Website.zip`, regenerated ~18s after the last commit and never committed.
- **Uncommitted work older than the last commit** (the §6 trigger): `docs/llm-assistant-spec.md` (Jul 14, untracked, **27 days** stale) and `.claude/launch.json` (Jul 13, untracked).
- **Build cruft:** nine untracked `.next-v5`…`.next-v12` + `.next-verify` directories are showing in `git status`. Worth a `.gitignore` line or a cleanup.

---

*Nothing was fixed beyond restarting the dev server, per task scope. Action items for the humans: (1) commit or shelve the Aug 9 app changes, (2) re-mirror demo + share package once they land, (3) eyeball the two 403 URLs, (4) gitignore `.next-v*`.*
