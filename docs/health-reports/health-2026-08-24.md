# VetPath Weekly Health Check — 2026-08-24

**Verdict: PASS with 3 flags** — build and data are clean; the dev server could not be restarted (unattended session), user-facing copy says "10 questions" but the assessment serves 11, and one source URL now 301-redirects.

## 1. Build — PASS
- `npx tsc --noEmit` → exit 0, no errors.
- No node dev server was listening on 3000–3002 (nothing to kill).
- `rm -rf .next` + `npm run build` → exit 0. Next.js 14.2.35, compiled successfully, 33/33 static pages generated, **0 warnings, 0 errors**. Shared first-load JS 87.5 kB.
- **FLAG — dev server not restarted.** `preview_start` refused: dev servers cannot be launched from unattended scheduled-task sessions (no one present to approve). A human needs to run `npm run dev` manually. `.next` currently holds a production build, so the next dev start will rebuild.

## 2. Data integrity — PASS (1 count mismatch, see flag)
| File | Expected | Found | Result |
|---|---|---|---|
| `data/stateBenefits.json` | 51 states | 51 | PASS |
| `data/sampleBenefits.json` | 11 categories, each w/ `lastVerified` + `sources` | 11, all present | PASS |
| `data/sampleCareers.json` | 15 careers w/ `onetCode` + `blsUrl` | 15, all present | PASS |
| `data/relocationMetros.json` | 29 metros, 28 with `official` | 29 metros, 28 official | PASS |
| `data/assessmentQuestions.json` | 10 questions | **11** | see flag |

- **FLAG — assessment count drift.** The file has 11 questions (`wins, pull, setting, structure, risk, physical, people, detail, speed, lead, place`). The 11th (`place`) has been in the file since the baseline commit `23b2ac3`, so the data is intentional and the check's expectation of 10 is stale. The app logic reads `ASSESSMENT.questions.length` dynamically (correct), but two marketing strings still hardcode "10 questions":
  - `app/dashboard/page.tsx:118`
  - `app/tools/page.tsx:17`
  - (`app/page.tsx:140` "6 of 10 actions" refers to plan actions, not questions — not affected.)
  Humans should decide: update the copy to 11, or make it read from `ASSESSMENT.questions.length` like `/pathfinder` and `/trust` already do. Also update this health check's expected count to 11.

## 3. Link-rot sample (8 URLs of 395) — PASS (1 redirect)
All 8 resolve to live, correct content. No 404s.
- ✅ `dva.iowa.gov/benefits/state-benefits` — Iowa DVA State Benefits
- ✅ `veterans.georgia.gov/education-training` — GDVS Education & Training
- ✅ `vets.ri.gov/` — RI Office of Veterans Services
- ✅ `nmhealth.org/about/ofm/ltcf/nmsvh/` — NM State Veterans' Home
- ✅ `va.gov/education/transfer-post-9-11-gi-bill-benefits/` — Transfer Post-9/11 GI Bill
- ✅ `huduser.gov/.../FY26_FMRs_revised.xlsx` — HTTP 200, valid xlsx (367 KB)
- ✅ `azdor.gov/.../PROPERTY_AZPropertyTaxExemptions.pdf` — HTTP 200, valid PDF (170 KB). *WebFetch got a 403 here; direct curl with a browser UA returns 200, so this is a WAF/user-agent block, not link rot.*
- ⚠️ **FLAG — `https://ova.dc.gov` → 301 → `https://communityaffairs.dc.gov/mova`.** The redirect target is live and correct (DC Mayor's Office of Veterans Affairs, now under Community Affairs). The link still works, but the stored URL is stale and should be updated to the new address before the redirect is eventually retired.

## 4. Deliverables freshness — PASS
- `demo/vetpath-demo.html` — 2026-08-23 22:19
- `share/VetPath-Interactive-App.zip` — 2026-08-23 22:21 (**newer**)
The share package is newer than the demo. Not stale; no rebuild needed.

## 5. Staleness — PASS
Oldest `lastVerified` is **2026-07-08 (47 days)**, well inside the 100-day window. Dates found: `2026-07-08` (10 entries + `stateBenefits` top level), `2026-07-09` (2), and month-only `2026-07` in `data/funding.json` and `data/reserves.json`. Quarterly refresh comes due around **2026-10-16**.

## 6. Git — FLAG
Last commit: `5d0694a` 2026-08-23 22:26 "Release refresh 2026-08-23: demo mirrors the link-rot fixes". Working tree has **no modified tracked files** — everything below is untracked and predates the last commit:
- **39 `.next-vNN/` build-artifact directories** (`.next-v5` … `.next-v44`, dating to Aug 12–20). `.gitignore` covers `.next/` and `.next-verify/` but **not** `.next-v*`, so these clutter every `git status`. Suggest adding `.next-v*/` to `.gitignore` and deleting the stale dirs.
- **7 prior health reports** (`health-2026-07-13` … `health-2026-08-17`) plus a stray `.devserver-2026-07-13.log.err` — never committed. Decide whether these belong in the repo or should be ignored.
- **Docs/config:** `docs/llm-assistant-spec.md` (Jul 14), `.claude/launch.json` (Jul 13), `tsconfig.ruletest.json` (Jul 27).
- **Assets:** `public/img/dvids-*-raw.jpg`, `public/video/career-summit-*`, and 8 files under `share/` (Jul 8–10). Verified these raw assets are **not referenced anywhere in `app/`, `components/`, or `lib/`** — they are source/scratch material, so leaving them untracked poses no deploy risk.

---
*Nothing was fixed; all items above are flagged for human action. Dev server restart was blocked by unattended-session policy.*
