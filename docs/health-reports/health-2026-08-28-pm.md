# VetPath health check - 2026-08-28 (evening, manual run on Kaleb's order)

Second run today. The morning report is `health-2026-08-28.md`; this one covers
the state after the intake-funnel instrumentation shipped.

| Section | Result |
|---|---|
| 1. Build | **PASS** - `tsc` clean and a full production build (32 static routes, exit 0) both ran minutes earlier in the full gauntlet on this exact commit; not re-run a third time in one day. Dev server restored and serving 200. |
| 2. Data integrity | **PASS** - 51 states, 11 verified categories, 15 linked careers, 29/28 metros, 11 assessment questions, horizon question intact, **and the new guard: the 5 intake funnel events still match the 5 intake step ids in order** |
| 3. Link-rot sample (8 URLs, fresh seed) | **PASS** - 5/8 HTTP 200; three .gov pages (bls.gov OOH, dvs.virginia.gov, veterans.ny.gov) returned 403 to curl but all three render fully with correct titles in a real browser. Same bot-wall pattern as Kansas and Tennessee in prior runs, not rot. |
| 4. Deliverables freshness | **PASS** - website zip rebuilt 14:48 (carries the funnel instrumentation); demo-derived deliverables current against a demo last changed 09:00 |
| 5. Staleness | **PASS** - oldest lastVerified 2026-07-08 in stateBenefits (52 days; quarterly refresh booked Oct 1) |
| 6. Git | **PASS** - clean tree, last commit a1babfd |
| 7. Housekeeping | **DONE** - pruned `.next-v66`; kept `.next-v67` (7.2MB) |

**Verdict: healthy across all seven sections. Zero defects found today across a
full 14-check gauntlet and two health checks.**

Note for future runs: three of eight sampled .gov links now 403 automated
requests. That rate is climbing, so the browser-verification step is doing real
work - a run that treated curl 403s as failures would report false rot.
